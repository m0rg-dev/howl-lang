static fn void a() {
}

class A<T> {
    static fn T static_method(T source) {
        return source;
    }

    fn T method(T source) {
        return source;
    }
}

static fn i32 Main() {
    a();
    A::<i32>.static_method(0);
    #[debug = "dumptypes"]
    (new A<i32>()).method(0);
    return 0;
}