module Test2;

class Vec<T> {
    *T value;

    fn void constructor() {
        
    }

    fn T __index__(i64 idx) {
        return self.value[idx];
    }
}

static fn i32 main() {
    let Vec<i32> foo;
    foo = new Vec<i32>();
    return foo[0];
}
