class A<T> {
    T value;

    fn void constructor(T value) {
        self.value = value;
    }

    fn B<T> get() {
        return new B<T>(self);
    }
}

class B<T> {
    A<T> value;

    fn void constructor(A<T> value) {
        self.value = value;
    }

    fn T get() {
        return self.value.get();
    }
}

static fn i32 Main() {
    let A test = new A(42);
    test.get().get();
    return 0;
}
