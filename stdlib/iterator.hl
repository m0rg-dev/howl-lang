interface Iterator<T> {
    fn bool hasNext();
    fn T next() throws OverrunException;
}

class OverrunException extends UncheckedException {
    fn void constructor(String message) {
        self.message = message;
    }
}
