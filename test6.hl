static fn i32 Main() throws IOException {
    let LineReader r = IO.stdinLineReader();
    while true {
        let Option<String> line = r.read();
        if(line.isSome()) {
            let i64 parsed = line.unwrap().toInt().unwrap();
            if parsed == 69 {
                IO.println("nice!");
            } else {
                IO.println(String.from(parsed));
            }
        } else {
            break;
        }
    }

    return 0;
}
