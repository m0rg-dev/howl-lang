static fn i32 Main() throws IOException {
    let Vec<String> v = new Vec();
    v.push("foo");
    v.push("bar");
    v.push("baz");

    for String s in v.iter() {
        IO.println(s);
    }

    return 0;
}
