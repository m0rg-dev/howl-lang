static fn i32 Main() throws IOException {
    let Vec<String> v = new Vec();
    v.push("foo");
    v.push("bar");
    v.push("baz");

    for Pair<i64, String> s in v.enumerate() {
        IO.println("index: " + s.getFirst() + " value: " + s.getSecond());
    }

    return 0;
}
