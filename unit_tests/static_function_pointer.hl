static fn void apply(fn void() source) {
    source();
}

static fn void test() {
    Test.ok("Function invoked through function pointer");
}

static fn i32 Main() {
    Test.init(1);
    apply(test);
    return Test.report();
}
