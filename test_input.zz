module TestInput;

fn static i32 puts(_rawptr_i8 s);
fn static _rawptr_i8 malloc(i32 size);

class String {
    _rawptr_i8 buffer;
    i32 length;

    fn void construct(String self, i32 length) {
        self.buffer = malloc(length);
        self.length = length;
        return;
    }

    fn void print(String self) {
        puts(self.buffer);
        return;
    }

    fn void setCharacter(String self, i32 pos, i8 char) {
        return;
    }
}

fn static i32 main() {
    let s String;
    s.construct(s, 1);
    s.setCharacter(s, 0, 65);
    s.print(s);
    return 0;
}